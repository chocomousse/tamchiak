<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Bill</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">

  </head>

  <body>
    <!-- NAVIGATION BAR -->
    <%= render 'layouts/header_current' %>

    <!-- CONTENT -->

    <div class="container">
      <h1><%= @channel.cname %> - Bill</h1>
      <button type="button"  class: "btn btn-primary" data-no-turbolink="true"><%= link_to "Home", join_or_create_path %></button>
      <button type="button"  class: "btn btn-primary" data-no-turbolink="true"><%= link_to "Back to Channel", current_channel_path %></button>
      <% if current_channel.user_id == current_user.id %>
      <button type="button"  class: "btn btn-primary" data-no-turbolink="true"><%= link_to "Send Bill", send_bill_path %></button>
      <% end %>
      <p></p>

      <% @bills.each do |t| %>
      <h4><%= t.user.name %> ordered <%= number_to_currency(@bills.sum(:total_price)[t.user_id]) %> worth of food</h4>
      <table class="table table-striped">
        <thead>
          <tr>
            <th>Category</th>
            <th>Sub-category</th>
            <th>Item Code</th>
            <th>Name</th>
            <th>Quantity</th>
            <th>Total Price</th>
          </tr>
        </thead>
        <tbody> 
          <% Order.where(user_id: t.user_id).find_each do |s| %>

          <p>
            <tr>
              <td><%= s.cat %></td>
              <td><%= s.subcat %></td>
              <td><%= s.item_code %></td>
              <td><%= s.meal %></td>
              <td><%= s.quantity %></td>
              <td><%= number_to_currency(s.total_price) %></td>
            </tr>
          </p>
          <% end %>

      </table>
      </tbody>
    <% end %>
    </div>
  </body>