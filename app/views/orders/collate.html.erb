<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Order this!</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="bootstrap.min.css">
  </head>

  <body>
    <!-- NAVIGATION BAR -->
    <%= render 'layouts/header_current' %>

    <!-- CONTENT -->

    <h1><%= current_channel.cname %> - Collated</h1>
    <button type="button"  class: "btn btn-primary" data-no-turbolink="true"><%= link_to "Bill", bill_path %></button>
    <button type="button"  class: "btn btn-primary" data-no-turbolink="true"><%= link_to "Home", join_or_create_path %></button>
    <% if current_channel.user_id == current_user.id %>
    <button type="button"  class: "btn btn-primary" data-no-turbolink="true"><%= link_to "Close Channel", @channel, :method => :get %></button>
    <% end %>

    <div class="container">

      <table class="table table-striped">
        <thead>
          <tr>
            <th>Category</th>
            <th>Sub-category</th>
            <th>Item Code</th>
            <th>Name</th>
            <th>Quantity</th>
          </tr>        
        </thead>
        <tbody>
          <% @relevant_orders.find_each do |t| %>
          <p>
            <tr>
              <td><%= t.cat %></td>
              <td><%= t.subcat %></td>
              <td><%= t.item_code %></td>
              <td><%= t.meal %></td>
              <td><%= @relevant_orders.sum(:quantity)[t.meal] %></td>
            </tr>
          </p>
          <% end %>
        </tbody>
      </table>
      <div>
        <h5>Delivery = <%= number_to_currency(@channel.delivery) %></h5>
        <h3>Total Cost (delivery charge included): <%= number_to_currency(@orders_in_channel.sum(:total_price) + @channel.delivery) %></h3>
      </div>
    </div>
  </body>
</html>